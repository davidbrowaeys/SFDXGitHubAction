name: Salesforce Multi-Environment Pipeline

on:
  pull_request:
    branches:
      - develop
      - 'release/**'
  push:
    branches:
      - develop
      - 'release/**'

jobs:
  # ----------------------------------------------------
  # 🔑 Global Helper: Determine Comparison Branches (Standard Job)
  # ----------------------------------------------------
  set_comparison_vars:
    runs-on: ubuntu-latest
    outputs:
      compare_branch: ${{ steps.compare.outputs.branch }}
    steps:
      - id: compare
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "::set-output name=branch::${{ github.base_ref }}"
          else
            echo "::set-output name=branch::HEAD^"
          fi
        shell: bash

  # ====================================================
  # QA STAGE (Validation & Deployment)
  # ====================================================

  # 1. QA Validation Trigger
  qa_validation_pr_trigger:
    runs-on: ubuntu-latest
    environment: QA
    needs: set_comparison_vars
    if: github.event_name == 'pull_request' && github.base_ref == 'develop'
    outputs:
      compare_branch: ${{ needs.set_comparison_vars.outputs.compare_branch }}
    steps:
      - run: echo "Triggering QA Validation..."
  
  # 1. QA Validation Call
  qa_validation_pr:
    name: 🧪 QA Validation (PR)
    needs: qa_validation_pr_trigger
    uses: davidbrowaeys/SafireForGitHub/.github/workflows/continuous-integration.yml@f3242958c10b404887431da4206f15a882f66bda
    with:
      target-env: 'qa'
      deployment-mode: 'DELTA'
      test-level: 'RunLocalTests'
      check-only: true
      compare-with: ${{ needs.qa_validation_pr_trigger.outputs.compare_branch }}
      auth-url-secret-name: 'AUTH_URL' # FIX: Pass literal secret name
    secrets:
      AUTH_URL: ${{ secrets.AUTH_URL }} # FIX: Pass the actual secret value
      
  # 2. QA Deployment Trigger
  qa_deployment_trigger:
    runs-on: ubuntu-latest
    environment: QA
    needs: set_comparison_vars
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    outputs:
      compare_branch: ${{ needs.set_comparison_vars.outputs.compare_branch }}
    steps:
      - run: echo "Triggering QA Deployment..."

  # 2. QA Deployment Call
  qa_deployment:
    name: 🚀 QA Deployment (Push)
    needs: qa_deployment_trigger
    uses: davidbrowaeys/SafireForGitHub/.github/workflows/continuous-integration.yml@f3242958c10b404887431da4206f15a882f66bda
    with:
      target-env: 'qa' 
      deployment-mode: 'DELTA'
      test-level: 'RunLocalTests'
      check-only: false 
      compare-with: ${{ needs.qa_deployment_trigger.outputs.compare_branch }}
      auth-url-secret-name: 'AUTH_URL' # FIX: Pass literal secret name
    secrets:
      AUTH_URL: ${{ secrets.AUTH_URL }} # FIX: Pass the actual secret value
